import { hexToHSL } from "./hexToHSL";

export function convertToHSL(colour) {
  return hexToHSL(colour);
}

// ----------------------------------------------------------------------------------------------------
